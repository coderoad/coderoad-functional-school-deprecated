var expect = require('chai').expect;
// loads file context to allow globals
var context = require('test-context');
var filePath = '../../../../../02-map.js';
context(filePath);

describe('next', function () {

  it('this test is unfinished', function () {
    return false;
  });

});
